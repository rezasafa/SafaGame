@model IEnumerable<SafaGames.Models.Categorys>

@{
    ViewBag.Title = "لیست تقسیم بندی";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <table>
                <tr>
                    <td>
                        <h1>
                            @item.Title
                        </h1>
                    </td>
                </tr>
                <tr>
                    <td>
                        @Html.ActionLink("ویرایش", "Edit", new { id = item.CategoryID }) |
                        @Html.ActionLink("نمایش", "Details", new { id = item.CategoryID }) |
                        @Html.ActionLink("حذف", "Delete", new { id = item.CategoryID })
                    </td>
                </tr>
            </table>
        </div>
    }
</div>
