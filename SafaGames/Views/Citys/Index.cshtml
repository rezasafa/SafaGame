@model IEnumerable<SafaGames.Models.Citys>

@{
    ViewBag.Title = "لیست شهر ها";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("جدید", "Create")
</p>
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <table>
                <tr>
                    <td>
                        <h1>
                            @item.countrys.Title -
                            @item.Title
                        </h1>
                    </td>
                </tr>
                <tr>
                    <td>
                        @Html.ActionLink("ویرایش", "Edit", new { id = item.CityID }) |
                        @Html.ActionLink("نمایش", "Details", new { id = item.CityID }) |
                        @Html.ActionLink("حذف", "Delete", new { id = item.CityID })
                    </td>
                </tr>
            </table>
        </div>
    }
</div>
